# react-exp12-dynamic-routing
To create a **dynamic route** in React using React Router that displays user details based on the **route parameter** (e.g., `/user/101`).    This demonstrates how to extract URL parameters and render data dynamically in React components.
